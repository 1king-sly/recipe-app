import 'package:flutter/material.dart';

class SingleRecipePage extends StatelessWidget {
  const SingleRecipePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold( );
  }
}